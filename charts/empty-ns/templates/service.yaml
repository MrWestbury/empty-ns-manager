apiVersion: v1
kind: Service
metadata:
  name: empty-ns-manager
  labels:
    kubernetes.wifu.co.uk/app: {{ include "empty-ns.name" . }}
spec:
  type: LoadBalancer
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    kubernetes.wifu.co.uk/app: {{ include "empty-ns.name" . }}
